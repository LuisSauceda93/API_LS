spring.application.name=API_LS
spring.datasource.url=jdbc:postgresql://localhost:5432/luissauceda
spring.datasource.username=luissauceda
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# LangChain4j OpenAI settings
# Uncomment and set your environment variable before running:
# OPENAI_API_KEY=...
#
# Example configuration:
langchain4j.open-ai.chat-model.model-name=gpt-4o-mini
# langchain4j.open-ai.chat-model.log-requests=true
# langchain4j.open-ai.chat-model.log-responses=true
